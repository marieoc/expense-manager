
export const initialState = {
    total: 0,
    expense: [],
    lastAction: ''
}


const calcReducer = (state, action) => {
    switch(action.type) {

        case 'add':
           
        case "delete":
           
        case 'total':
          
        default:
            return state;
    }
}

export default calcReducer